"""EVIE OS G1 — Core State (Projects / Goals / Steps / Commitments).

Canonical operational truth for goal-directed state. Durable history lives in
the canonical `events` table; recall lives in the Memory OS. This package owns
the state transitions that emit those events.
"""
